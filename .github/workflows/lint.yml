name: Lint

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run BrightScript lint
        run: |
          make lint || true

      - name: Check formatting
        run: |
          make check || true

      - name: Validate XML files
        run: |
          make validate-xml || true
